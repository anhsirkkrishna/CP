If the goal is deduplication from a vector, the fastest approach is often:

sort(v.begin(), v.end());
v.erase(unique(v.begin(), v.end()), v.end());

O(n log n) but with much lower constant factors than std::set, since vectors are cache-friendly.

https://codeforces.com/contest/2203/problem/B

I want to see how many digits I can convert to 0 untill the sum becomes less than x.
One approach was to consider 1st as 1 or 1st as the current and then sort the rest and
then do the calculations. The other way was to store all the values each digit can loose.
So, ai for all and ai-1 for i = 1. This would be less complicated than doing it 2 times.
This is way less complicated.
